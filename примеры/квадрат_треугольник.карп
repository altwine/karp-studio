алг КвадратТреугольник

поднять_перо()
домой()
очистить()
опустить_перо()

цвет_пера(255, 0, 0)
толщина_пера(3)

счётчик1 := 0
пока счётчик1 < 4:
    вперед(100)
    направо(90)
    счётчик1 += 1

цвет_пера(0, 255, 0)
счётчик2 := 0
пока счётчик2 < 3:
    вперед(150)
    направо(120)
    счётчик2 += 1
